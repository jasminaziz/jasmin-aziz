# macOS
.DS_Store

# Design archive — 27MB download, not part of the site
design-download.tar.gz

# Full design system source — reference only, not deployed
jasmin-aziz-design-system/

# Claude project files — settings stay local, CLAUDE.md is tracked so the
# project context survives a clone. Keep commercially sensitive strategy OUT
# of it: this repo is public. Strategy lives in ~/AI Work/memory/.
.claude/*
!.claude/CLAUDE.md

# Local font files — fonts are loaded from CDN (Google Fonts + Fontshare)
Fonts/

# Misc
*.orig
.vercel
.env
.env.local
