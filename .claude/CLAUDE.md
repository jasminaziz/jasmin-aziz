# Jasmin Aziz Site — Claude Instructions

## Deployment
- GitHub: https://github.com/jasminaziz/jasmin-aziz (public)
- Production: https://www.jasminaziz.co.uk. **`www` is the canonical host** on every surface: canonical tags, og:url, og:image, sitemap, robots.txt, llms.txt and JSON-LD. The bare domain redirects to it in one hop via `vercel.json` (a 308, because `permanent: true`; set `statusCode: 301` instead if a 301 is ever required). Custom domain live 2026-06-07. Any new absolute URL uses `www`.
- Vercel alias: https://jasmin-aziz.vercel.app (also works)
- Static HTML/CSS/JS — no build step, no framework
- Push to main → automatic Vercel redeploy

## Key facts
- Homepage file is `index.html` (renamed from "jasminaziz-homepage v2.html")
- Fonts load from CDN — never reference the local Fonts/ directory
- Contact form wired to Resend via api/contact.js — branded HTML notification to hello@jasminaziz.co.uk and auto-reply to enquirer
- Analytics: NONE. Google Analytics was removed site-wide on 2026-08-22 and nothing installed in its place. The site sets no tracking cookies and shows no consent banner; the Legal page says so. Do not reinstate analytics without an explicit decision.
- Scroll-to-top: scroll-top.js loaded on all six pages; styles in site.css; hidden at ≥901px
- vercel.json: cleanUrls + non-www → www 301 redirect (host-matched) + security headers on all routes, including a Content-Security-Policy and X-Frame-Options: DENY (added 2026-08-22). CSP gotcha: Fontshare serves its stylesheet from api.fontshare.com but its font files from cdn.fontshare.com — font-src must name the cdn host or Chillax fails sitewide.
- .vercelignore: keeps project/reference files out of the deploy (`*.md`, `reports/`, `tasks/`, the build handover .html). Verified to apply to Git-based deploys. Anything non-site added to the repo MUST be covered by it — before this existed, STATUS.md, the design spec and two dev files were live and crawlable on production.
- **Every service with a contact button needs a matching form option.** The site has no analytics, so the enquiry email's "Enquiry type" is the only way to know which service prompted an enquiry. A service whose button goes to plain `/contact` arrives labelled "I'm not sure yet" and its demand is invisible. Add the option in `contact.html` and the label in `api/contact.js` (same value, same position) and point the button at `/contact#<value>`.
- **The AI service description is a two-file contract.** `ai.html` carries a `Service` block and the `services.html` `@graph` carries the same service; both share `@id` `https://www.jasminaziz.co.uk/ai#service` and point at `/ai`, so search reads them as one entity. Their `description` fields must stay identical, or one entity is described two ways. Change both in the same commit.
- Contact form option values are a two-file contract: `/contact#<value>` preselects via `preselectFromHash()` in contact.html, and `SERVICE_LABELS` in api/contact.js maps the same values to the enquiry-email label. Renaming an option means changing BOTH, or the notification email prints the raw value. After any change, re-check every `/contact#…` anchor against the option list.
- api/contact.js escapes user input (`escapeHtml`) before it enters the HTML notification email. Keep it that way. Still outstanding: honeypot and rate limiting.
- Internal links: all .html hrefs updated to clean URL paths (/services, /about, /contact, /legal, /)
- SEO: complete — meta titles, descriptions, canonical tags, og:url, JSON-LD, robots.txt, sitemap.xml, llms.txt all done and pushed
- AEO: complete — Person schema enhanced (description, knowsAbout, hasOccupation) on index.html; FAQPage + Service schemas on services.html
- 404 page: 404.html in project root — branded, Chillax heading, 30s auto-redirect to /
- CSS architecture: three files. `site.css` — shared styles (nav, footer, tokens, body, scroll-top, who-section, pills). `homepage.css` — homepage-only components (hero, hero-grid, diff, proof cards, stack pills, etc.); also overrides `--sv: clamp(44px, 7vw, 76px)` and adds `--h1-size: 68px`. `index.html` loads both in order. Inner pages load `site.css` only. `services.html` has its own inline `<style>` block for page-specific components — new services CSS goes there, never in site.css.
- Hero portrait: `<picture>` element in `.hero-portrait-wrap`. Above 900px: `assets/jasmin-headshot.png` (portrait, 3/4 oval, border-radius: 999px) in a `minmax(0, 1fr) 300px` grid, with the H1 at `clamp(2.75rem, 5vw, 68px)` so "Strategic marketing" always fits its column. At 900px and below the hero stacks with `assets/jasmin-headshot-landscape.jpg` above the H1 (2/1 capsule, then 3/2 at ≤640px). Fixed 2026-09-12: the old fixed-size H1 and `1fr` track clipped the portrait off-screen from 641px to ~1128px. Check hero changes at 641, 768, 900, 1024 and 1130, not just a phone and 1440.
- Services page structure (rebuilt 2026-09-12): every `.svc-block-body` has exactly two children, `.svc-main-col` (the copy) and `.svc-side` (the "Right for" line, then the CTA pill), in a `minmax(0, 1fr) 240px` grid. The engagement-shape blocks use the same `.svc-side` for their CTA. No spanning and no implicit rows: the previous `grid-row: 1/-1` collapsed to one row and put every CTA under the copy for months. Mobile needs no reordering; DOM order reads copy, Right for, CTA. Pull quotes are gone sitewide by Jasmin's ruling; do not reintroduce them.
- About-bridge section (index.html) is text-only — portrait was relocated to the hero. Do not add an image back here.
- `--ink-muted` (#706D68) is calibrated for ink at 45% opacity only. Body copy uses `color: var(--ink)` directly. Do not use `--ink-muted` as a general muted text token — it is lighter than 68–76% opacity muting and will look wrong on body copy.
- Services architecture rebuild: `services-descent` branch (2026-06-18), unmerged and STALE — do not merge as-is (see warning below). Four moves: (1) entry CTA after intro, (2) audit "Start here" with periwinkle accent, (3) shapes heading parity (tier-title class, Fractional/Advisory/Per project in anchor bar), (4) periwinkle closing band on How an engagement starts.
- Reports (now committed, and .vercelignore'd so they never deploy): `reports/site-security-2026-06-10.md` and `reports/site-stranger-2026-06-15.md`. Read the security one before any security-related work. Status as of 2026-08-22: escapeHtml, CSP and X-Frame-Options DENY are DONE; the unpkg/tweaks-panel finding is closed by deletion; both "accepted trade-offs" are retired with GA4. Only honeypot + rate limiting remain open. Stranger audit still live on three points: the fractional definition sits ~1,800 words into Services, the Fractional shape block has no "Right for" component, and Campaign copy names deliverables not outcomes — note its proposed fix (extend the homepage hero lead) is ruled out, the hero does not change.
- **`services-descent` merge warning (2026-08-24).** The branch predates the AI page, the GA4 removal and the security fixes. Merging it as-is would (a) delete `ai.html` entirely and (b) re-narrow `index.html:153` — its version reads "across hospitality, fundraising, early years, wellness, events, and cultural programming, inside charity, heritage, and third-sector organisations", which is narrower than what is live and would silently undo any buyer-definition widening. Rebase onto main and take only the four services moves; never fast-forward or merge the whole branch.
- AI page: `/ai` (ai.html) — "AI, trust and communications". LIVE on main since 2026-08-22. It was rebased off services-descent onto main and shipped independently; services-descent remains unmerged and unrelated.
- Nav order (all pages): Home · Services · AI · About · Contact.
- **The Edit is named "The Edit"** in body copy, `legal.html` and `llms.txt`. The six footer "Elsewhere" entries keep "The Edit AI", where the "AI" does the descriptor's job in a bare link list beside Substack and LinkedIn. The property never calls itself "The Edit AI" anywhere, including its schema.org WebSite name.
- **Never put counts from The Edit in this site's copy.** Tool numbers, stack counts and news totals are live Sheet data, public the moment a row changes, with no deploy and no review. This site is static and cannot track them, so any hardcoded number is wrong by construction on the page whose job is showing currency. Describe the practice, not the tally: "every tool carries the date it was last checked" survives, "23 tools" does not.
- **The Edit's audience phrase never shortens to "charity" alone.** Nominal "charities, cultural organisations and heritage" where the audience is the object; adjectival "charity, cultural and heritage" where it modifies a noun. If a headline must compress, drop *cultural* and keep *heritage* — The Edit's own precedent (its /tools title reads "Charity & Heritage Comms"), and heritage is the word carrying the cathedral, the archive and the local-authority museum service. Never compress in body copy. Also: "been through the checks", never "passed the checks".
- **GEO audit (added 2026-09-10) has an open credibility question.** The GEO product calls llms.txt "a file nobody reads", and this site publishes one (`llms.txt`). A GEO-literate buyer who reads the pitch and then inspects the site can see the gap. Not acted on: whether to keep or remove it is Jasmin's call. The GEO copy on the site describes the approach and the evidence, never results, and never names a price: keep it that way, matching the training suite. Trigger for the line: Media Trust's "GEO for charities: Getting found in AI search and answers", Thursday 12 November 2026 (verified on mediatrust.org 2026-09-10).
- **The 24 August buyer widening STANDS. Reviewed and upheld 2026-09-03.** `BRAND_Architecture-Positioning-Audit_v1` (3 Sept, in Cowork CLAUDE OUTPUTS/BRAND) recommends reversing it as an untested assumption stacked on another. Jasmin declined. The reasoning: **The Edit is the niche surface and has already pivoted to charity, cultural and heritage; the consultancy site does not have to make the same move.** Two properties, two jobs, deliberately. Do not narrow the site to match The Edit. The audit's own watch condition is the right one: revisit only if a commercial enquiry actually arrives, and then it is the fifth line's copy that needs a second version, not the site.
- **Cadence and schedule claims are self-updating facts and this site now makes none.** "Weekly, every Tuesday" described the Substack on `index.html`, `about.html` and `llms.txt` and was false on both counts: 14 posts, last on 17 July 2026, 1 of 14 on a Tuesday. Removed 2026-09-03 by deletion rather than replacement, because any schedule claim goes stale unwatched. Describe the writing, not the schedule. Same principle as the no-counts rule for The Edit.
- **Services order is deliberate: Audit 01, AI 02, GEO 03, Brand 04, Campaign 05, Content 06.** AI moved up from fifth on 2026-09-03 because it is the growth product against evidenced sector demand; it is not first because the page's own logic depends twice on Communications audit being the entry point. GEO audit was added at 03 on 2026-09-10, next to AI, because it belongs to the same AI, trust and communications family and sixth would have repeated the "fifth on a list" problem. The services move together across FOUR places carrying all six: `services.html` blocks and numbering, the anchor bar, the `index.html` stack-pill list, and the `contact.html` select with its `SERVICE_LABELS` twin in `api/contact.js` (GEO's value is `geo-audit`, matching its block id and the `/contact#geo-audit` CTA). **Two places deliberately carry only the original five and must NOT gain GEO**: the JSON-LD `@graph` in `services.html` and the services line at `llms.txt:8`. That is Jasmin's ruling: the GEO product itself argues schema markup and llms.txt are not GEO levers, so the site does not use them to sell it. A grep that finds six services in some places and five in others is looking at a decision, not unfinished work.
- **The site runs TWO REGISTERS on audience, deliberately. Do not "fix" this.** Ruled 2026-08-31. Human-facing prose keeps **"values-led organisations"**: `index.html:135`, `about.html:436`, `index.html:7` meta description, and the shared OG/Twitter description in all ten tags across index, services, about, contact and legal. Machine-readable surfaces carry the precise buyer definition, **"organisations that hold sensitive data and answer to a board"**: the Person and hasOccupation JSON-LD at `index.html:32` and `:51`, `llms.txt:3`, and the FAQPage answer at `services.html:420`. The reasoning: prose surfaces speak in Jasmin's voice and "values-led" carries the warmth that guards the corporate anti-reference, while anything answering the machine question "who does she work with" needs the precise definition. A grep for `values-led` will return around thirteen instances and look like unfinished widening. It is not. `ai.html` is a third case: scoped by resourcing, not by either phrase.
- **The homepage practice definition is credential, not scope** (`index.html:153`, recast 2026-08-31). "I build and lead marketing and communications functions from inside the team" defines the practice unscoped; "I've done that across charity, heritage, cultural and community organisations" is past-tense evidence. Do not merge those two sentences back together, and do not move the sector list into the present tense: that is what made it the sharpest gate on the site before it was fixed.
- **Inline prose links have no CSS rule anywhere on this site.** The pattern is an inline `style="color:var(--cobalt);text-decoration:none;"`, used on prose links across the pages. Omit it and the link renders browser-default `#0000EE`, underlined. Two Edit links shipped that way on 2026-08-31 before being caught.
- Career length is **ten years**, stated in `about.html:7`, `about.html:476` and `llms.txt:10`. Jasmin's professional career began in **early 2016** (confirmed 2026-09-10), so it becomes **eleven in early 2027**: re-confirm then and update all three together. History: "eight" from launch on 2026-06-06, which was an understatement from the day it was written; "nine" on 2026-08-24; "ten" on 2026-09-10 once the start date was confirmed.
- **Self-updating facts need revalidation, not inheritance.** "Ten years", the "six years" on the multi-site community organisation engagement, and any "most recently" claim all drift with time. Any figure about Jasmin is checked against a dated fact she has given, never against an earlier document or session. "Eight years" was wrong from the day it was written, not a true fact that aged, and it survived three agent audits because none of them check facts about her, only code and copy. Re-confirm these with her at each wrap; never carry them forward unasked.
- The AI services block (now 02) is a pointer block linking to `/ai`; it keeps `id="ai-training"` so existing anchors resolve.
- About: one sentence added to the Background paragraph on Jewish communal career background — the only such reference on the site; rest of the site (including the AI page) stays sector-neutral.
- Design rule: never stack two tinted `.who-section` bands directly adjacent — their wave pseudo-elements smear at the shared seam. Always separate tinted bands with a cream section (see ai.html: cream → beige → cream → periwinkle).
- See SCRATCHPAD.md for current status and next steps
- See tasks/lessons.md for corrections from previous sessions
- Claims register: kept privately at `~/.claude/steward/claims-jasmin-aziz.md` (claude-config), not in this public repo. It lists every claim that lives on more than one surface, depends on another property or the code, or can go stale by itself, with Jasmin's exceptions and parked findings. The site-steward agent checks it on the 1st and 15th and at /wrap. Changing a claim means changing every surface its `Lives on:` line names, or the register.

## Design Context

### Users
Potential clients — decision-makers at organisations that hold sensitive data and answer to a board. Charity, cultural and heritage organisations are one instance of that shape, not the definition of it. **This repo is public: the named target sectors and the commercial rationale are deliberately not recorded here. They are in `~/AI Work/memory/decisions.md`, 2026-08-24 entry — read it before any positioning or copy work.** They arrive mid-decision — vetting, not browsing — assessing whether Jasmin understands their world and whether they'd want to work with her. Job to be done: form a confident "yes, let's talk", or rule her out efficiently. (Buyer definition widened 2026-08-24; previously "charity and purpose-led organisations".)

### Sector language rule (2026-08-24)
Charity/heritage/third-sector language on this site does one of two jobs. They are not the same job.

- **CREDENTIAL** — named sector experience, nine years, proof she has actually done the work. This earns trust and it stays. Removing it makes the site weaker, not wider.
- **GATE** — service definitions scoped to charities, present-tense audience statements naming only the third sector, examples citing one kind of organisation. This costs buyers who share the problem.

The same words do either job depending on where they sit. Rules of thumb: past tense, or labelled "Background", is usually credential; present-tense scoping of the practice is usually a gate. Widen where the site defines the *problem*; do not widen where it names *who she has worked for*, because there is no non-charity client evidence yet. Full instance-by-instance inventory: session 2026-08-24.

### Brand Personality
**Grounded. Rigorous. Human.** Credibility is the foundation; warmth is the differentiator.

### Aesthetic Direction
Reference: personal brand / thought leader (Anne-Laure Le Cunff, Tiago Forte). Typographically confident, intellectually engaged, warm without being casual.

Anti-references: generic agency/template, corporate/buttoned-up, startup/tech-bro, overly personal/informal.

### Design Principles
1. **Credibility before charm.** Expertise and substance first; warmth softens, never leads.
2. **Editorial over decorative.** Typography does the heavy lifting — contrast, weight, spacing before adding visuals.
3. **No templates, no defaults.** Every component should feel considered, not borrowed wholesale.
4. **Accessible by default.** WCAG AA is the floor. Pre-blended text tokens always (--ink-muted, --ochre-text, --periwinkle-text). Never opacity on text. 44px touch targets. Reduced motion support.
5. **Restrained palette, deliberate usage.** Cobalt = action. Ochre = warmth/attention. Periwinkle = secondary emphasis. No new colours. Solve constraints with type or space.
